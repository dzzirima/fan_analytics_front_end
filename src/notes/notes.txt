json-server -watch data/db.json --port


// api.getGlobalhash(userName,password)
      // console.log(userName + password)
      // console.log(api.loginStatus())
      let  globalHash = JSON.parse(api.getGlobalHashFromLocalStorage()).hash